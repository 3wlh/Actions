name: Actions GitHub Push CNB

on:
  #push:
    #branches: [main]
  #schedule:
    #- cron: "0 16 * * 0"
  workflow_dispatch:

jobs:
  job_push:
    uses: 3wlh/Actions/.github/workflows/Call_GitHub_Push_CNB.yml@main
    with:
      user: '3wlh'
      repository: 'Source_E'