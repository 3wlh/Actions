name: cs

on:
  workflow_dispatch:
    
jobs:
  jods_Actions:
    name: cs
    runs-on: ubuntu-latest
    steps:
      - name: cs
        uses: 3wlh/actions/CNB/upload-releases@main
        with:
          token: ${{ secrets.TOKEN_CND }}
          tag_name: "v1.0.0"